============
Installation
============

At the command line::

    $ pip install can4python

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv can4python
    $ pip install can4python

